# Copied from rootSolve package

Copied from rootSolve package

## Usage

``` r
.uniroot.all(
  f,
  interval,
  lower = min(interval),
  upper = max(interval),
  tol = .Machine$double.eps^0.2,
  maxiter = 1000,
  n = 100,
  ...
)
```
